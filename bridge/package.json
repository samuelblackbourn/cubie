{
  "name": "cubie-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mirrors Virtual-Office state onto Cubie's posture, face and LEDs.",
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "start": "node --experimental-strip-types src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  }
}
